print(type(x))
- to print out type of variable of 

first_list = [123, "hello"]
- python list 

first_list = (123, "hello")
- python tuple

first_dict = {"name": "junman", "gender": "male", "age": 24, "interst": ["devops", "sre", "programming"]}
- python dictionary

x = input("input ur name: ")
- input to request for user input 

x = ["h", "e", "l", "l", "o"]
for i in x:
    print(f"{i} is the word")

break & continue
- used to break the code or skip 

x.capitlise()/ x.upper()/ x.lower()
- to cap first word of a sentense 

x.find("word")
- to find the index of word in a sentence

print(".",join(x))
- to join the lines in x

x.append("hello")
- to add hello to the end of x

x.extend(["elo", "hihi"])
- to extend list of x

x.insert(2, "hello")
insert at index 2

codes to take note 
- add 1-10 (2 methods)
- delimit thousands with ,
- reverse a string 
- fibonacci seq
- if divisble by 5 print x divisible by 7 print y. bla bla 
- angle between circle